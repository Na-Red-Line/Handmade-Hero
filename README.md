# Handmade Hero

Learn about the [Handmade Hero](https://mollyrocket.com/#handmade)


## 音频

频率：一秒钟震动的次数  

音频写入缓冲区数据

第一种情况，写入区域跨越边界

|---------------------------------|  音频缓冲区
           ^          ^
    		播放头       写入头
|---------------------------------|
           ^          ^
---写入区---|--安全区--|---写入区---|

数学运算  w(写入头) > p(播放头)


第二种情况，写入区域在中间

|---------------------------------| 音频缓冲区
           ^          ^
    		写入头      播放头     
|---------------------------------|
           ^          ^
---安全区---|--写入区--|---安全区---|

数学运算  w(写入头) < p(播放头)


波形、相位


## 性能计算

耗时、FPS


## 平台无关控制输入输出

基于范围过渡变化，最终值


## 内存池化技术
想办法从操作系统那里请求到真实的物理内存


## 文件IO

流式传输故障点太多 我们知道文件如何分割，所以一次性读取文件或一个大文件块

安全写入存档，A B 文件交替写入


## 编译器优化

一些编译器内置方法和CPU内置指令

`_BitScanForward`


## 游戏手柄死区

手柄硬件摇杆值实际上并不是从 0 开始变化，会有一个死区无法检测，实际上是突然从死区值进行变化的

## 游戏代码热更新

DLL 链接加载

如果不是静态链接需要注意跨边界内存分配问题

通过 DLL 更新时间判断是否需要重新加载可以做到一帧延迟热更新

## 调试游戏资源文件

遇到一个不熟悉的文件格式  
可以创建一个相关格式文件，然后清除所有像素，在四个对角设计像素（红蓝绿白）等观察数据格式进行调试

Alpha 通道混合减少锯齿，使边缘更平滑

## 引擎设计

先实现一些简单的需求，明白自己的引擎需要做什么，再进行设计开发

## 相机

1. 相机不移动，角色移动
2. 角色移动，相机不移动

## 代码设计

#define 重新定义关键字目的是为了 可搜索性

## 游戏中常用的数学与物理

1. 线性混合 `A + x(B - A)`

> 算术 复数 代数 欧几里得几何 三角学 微积分 线性代数 区间算术  
> 偏微分方程 常微分方程 非欧几何 闵可夫斯基代数  
> 控制理论 拓扑学 图论 运筹学 概率与统计 离散数学

## 游戏设计

虽然游戏机制设计重中之重，但美术音乐这才是吸引更的玩家的基础，所以要做好一款游戏，要努力打磨的细节有很多。

从基础的物理化学规则出发再进行自定义微调可以做到使游戏拥有自定义物理引擎和符合人类直觉的效果，而不是忽略物理化学等知识自定义所有一切。

生命分流、附魔、多人游戏

## 运动

### Markdown 公式写法

行内公式：$v = v_0 + a \Delta t$

块级公式：

$$
x_{t+\Delta t} = x_t + v_t \Delta t + \frac{1}{2} a_t \Delta t^2
$$

### 移动与回弹

- 使用加速度驱动移动：输入方向 -> 期望加速度 -> 速度 -> 位置。
- 位置积分只能做一次，否则会把加速度当成速度再加一次，产生过冲与回弹。
- 常见阻尼写法：$a = a_{input} - k v$，其中 $k$ 控制刹车强度与回弹感。

### 公式与推导

速度更新：

$$
v_{t+\Delta t} = v_t + a_t \Delta t
$$

位置更新：

$$
x_{t+\Delta t} = x_t + v_t \Delta t + \frac{1}{2} a_t \Delta t^2
$$

推导思路（常加速度）：

$$
v(t) = v_0 + a t
$$

$$
x(t) = x_0 + \int_0^t v(\tau)\,d\tau = x_0 + v_0 t + \frac{1}{2} a t^2
$$

离散化时，用 $\Delta t$ 近似连续时间，得到上面的两条更新公式。

### 推荐学习资料

- 经典力学基础：位移-速度-加速度关系、匀加速度运动。
- 数值积分：Euler / Semi-implicit Euler / Verlet。
- 游戏编程数学：向量、坐标系、插值与稳定性分析。

### 碰撞反弹

$$
\mathbf{v}' = \mathbf{v} - 2(\mathbf{v}\cdot\mathbf{n})\mathbf{n}
$$

代码对应：

```cxx
dPlayerP = dPlayerP - 2 * inner(dPlayerP, r) * r;
```

其中：

- $\mathbf{v}$：碰撞前速度（`dPlayerP`）
- $\mathbf{n}$：墙面法线（`r`，需是单位向量）
- $\mathbf{v}\cdot\mathbf{n}$：速度在法线方向上的分量大小

直观理解：

- 原公式（系数 1）是“去掉朝墙的速度分量”，结果更像“贴墙滑动/停止”。
- 新公式（系数 2）是“把朝墙分量翻转过去”，即镜面反射，所以会反弹。

### 反弹公式的最小推导

内积 外积 点积

把速度拆成两部分：

$$
\mathbf{v}=\mathbf{v}_{\parallel}+\mathbf{v}_{\perp}
$$

其中法线方向分量为：

$$
\mathbf{v}_{\perp}=(\mathbf{v}\cdot\mathbf{n})\mathbf{n}
$$

切线方向分量为：

$$
\mathbf{v}_{\parallel}=\mathbf{v}-\mathbf{v}_{\perp}
$$

碰撞反弹时：切线不变，法线取反：

$$
\mathbf{v}'=\mathbf{v}_{\parallel}-\mathbf{v}_{\perp}
$$

代入得到：

$$
\mathbf{v}'=(\mathbf{v}-\mathbf{v}_{\perp})-\mathbf{v}_{\perp}=\mathbf{v}-2\mathbf{v}_{\perp}=\mathbf{v}-2(\mathbf{v}\cdot\mathbf{n})\mathbf{n}
$$

如果想做“有损耗反弹”（不那么弹），可用恢复系数 $e\in[0,1]$：

$$
\mathbf{v}' = \mathbf{v} - (1+e)(\mathbf{v}\cdot\mathbf{n})\mathbf{n}
$$

- $e=1$：完全弹性（最弹）
- $e=0$：不反弹（只移除法线分量）

### 注意点（为什么有时看着不对）

- 法线方向必须正确且最好单位化，否则反弹方向/力度会异常。
- 角落碰撞可能同帧触发多个方向，法线被覆盖会抖动。
- 只改速度不做位置修正时，可能“卡墙”或连续抖动。

### 数学零基础学习路线（按顺序）

1. **向量入门（最优先）**
    - 什么是向量、长度、单位向量。
    - 点积（内积）代表“投影大小”。
2. **高中物理最小集**
    - 位移、速度、加速度三者关系。
    - 匀加速度公式（你代码里已经在用）。
3. **游戏中的时间离散化**
    - 每帧更新（$\Delta t$）是什么。
    - 为什么会有数值误差和穿透。
4. **碰撞基础**
    - 法线、切线、投影分解。
    - 反射公式与恢复系数 $e$。
5. **进阶（后面再学）**
    - Semi-implicit Euler、AABB 扫掠碰撞、连续碰撞检测。
